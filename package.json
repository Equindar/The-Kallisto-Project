{
  "name": "the-kallisto-project",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "turbo": "^2.8.7",
    "prettier": "3.4.2"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}